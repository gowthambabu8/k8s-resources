apiVersion: v1
kind: Service
metadata:
  name: Service
spec:
  selector:
    # labels are used for selector
    project: roboshop1
    env: dev
  ports:
    - protocol: TCP
      port: 80 # service port
      containerPort: 80 # container port
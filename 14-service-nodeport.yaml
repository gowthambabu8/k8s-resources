apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  type: Nodeport
  selector:
    # labels are used for selector
    project: roboshop1
    env: dev
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # container port